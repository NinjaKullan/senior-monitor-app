<?xml version="1.0" encoding="utf-8"?>
<!--
  Spec 014 §4.3: every permission the app will ever hold, all declared here.
  Nothing else: no whereabouts, no usage stats, no contacts, no SMS. The audit
  test (src/test ManifestAuditTest, and the auditXxxManifest Gradle task over
  the merged manifest) fails the build if that ever changes.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ package visibility: the OEM battery/autostart pages that
         "Show me" can open (OemSettings.kt). Listing a package here grants
         nothing; it only lets resolveActivity see whether the page exists. -->
    <queries>
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.samsung.android.sm_cn" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.oneplus.security" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.huawei.systemmanager" />
    </queries>

    <!-- The step counter is how `motion` is read (§3). Not required, so the
         app still installs on a phone without one; motion is simply never sent. -->
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />

    <application
        android:name=".KettleApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.Kettle"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- §4.1: one foreground service, type `health` (it reads the step
             sensor and holds ACTIVITY_RECOGNITION). The DECISIONS build notes
             record why, and the `specialUse` fallback if Play rejects that. -->
        <service
            android:name=".KettleService"
            android:exported="false"
            android:foregroundServiceType="health"
            android:stopWithTask="false" />

        <!-- §4.2: RECEIVE_BOOT_COMPLETED restarts the service and the worker. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
